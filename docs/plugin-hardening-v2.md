# Plugin hardening v2 (superseded)

This plan targeted the QuickJS/TS `next-code-plugin-*` stack. That product was
deleted in favor of Face `/plugins` + ACP handlers under `~/.next-code`.

See [plugins.md](./plugins.md) and
`docs/plans/PLAN-20260721-plugins-grok-vs-nextcode.md`.
