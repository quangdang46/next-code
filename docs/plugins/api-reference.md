# Plugin API reference (removed)

The QuickJS/TypeScript plugin API (`next-code-plugin-core` /
`next-code-plugin-runtime`) was removed. Plugins are now Grok-style directory
bundles managed from Face `/plugins`.

See [../plugins.md](../plugins.md).
