# Deepen — Tools ABI v1 (`[[tools]]`) (2026-07-22)

**Priority:** P2 · Phase 3  
**Parent:** Master plan Phase 3  
**Open Q:** argv in Phase 3 vs MCP-only until then?

---

## Stance

**Prefer MCP-first** for new third-party tools until argv tool bridge is reviewed (schema + allowlist + trust). Phase 3 lands `[[tools]]` argv when policy ready.

## Shape (when enabled)

```toml
[[tools]]
name = "deploy_status"
input_schema = "schemas/deploy_status.json"
runner = { argv = ["./bin/deploy-status"] }
```

- Host allowlists / permissions still apply.
- No in-process `registerTool` (Pi) / no Bun plugin tools.
- Providers stay core/gateway — not guest-register.

## Exit criteria

- [ ] One argv tool callable by model without core PR (after review).
- [ ] Deny path + timeout + trust documented.
- [ ] Open Q resolved in changelog / this file.
