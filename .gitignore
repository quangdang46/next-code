/target
Cargo.lock
__pycache__/
ios_simulator_screenshot.png
/.wrangler/
/tmp/
.beads/
.claude/worktrees/
# Parallel-agent git worktrees live here; never track them (gitlinks with no
# .gitmodules break `actions/checkout --recursive` in CI).
.worktrees/
# Local onboarding sandbox + auth fixtures. May contain REAL OAuth tokens copied
# from $HOME (see scripts/onboarding_sandbox.sh seed-real-logins). Never commit.
/.tmp/
/.next-code/generated-images/
/.jcode/generated-images/
/telemetry-worker/backups/
# Local session artifacts (opencode IDE cache, OMO continuation logs, session markdown dumps)
.opencode/
.omo/
session-ses_*.md
target-ttest/
target-tttest*
target-tttest*/
libnext_code_base.rlib
libjcode_base.rlib
.DS_Store
.fastembed_cache/

# bv (beads viewer) local config and caches
.bv/

# Stray experiment/debug artifacts at the repo root. Real assets live under
# assets/, docs/, ios/, and tests/ and are committed explicitly.
/*.log
/*.avif
/*.mp4
/*.png
