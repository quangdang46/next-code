# GJC Dogfood — next-code start prompt

You are working on quangdang46/next-code — a Rust fork of xai-grok with refactored UI.

## Project rules
- AGENTS.md in repo root has development conventions (commit as you go, cargo check, remote build)
- This is a Rust workspace with many crates under crates/
- Install notes: ~/.local/bin/next-code, builds in ~/.next-code/builds/

## Workflow this session
1. Research: read AGENTS.md + relevant crate docs before editing
2. Implement in a worktree branch
3. Prefer `cargo check -p <crate>` for fast iteration
4. After changes: `cargo test -p <crate>` for focused tests
5. Commit, push, create PR with `gh`
6. Wait for CI, then comment MERGE_READY

## Constraints
- Do NOT merge unless user says so explicitly
- PRs should be draft unless CI is expected green
- Always reference the issue number in PR body with Closes #N
