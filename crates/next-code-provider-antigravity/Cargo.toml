[package]
name = "next-code-provider-antigravity"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
next-code-provider-gemini = { path = "../next-code-provider-gemini" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
