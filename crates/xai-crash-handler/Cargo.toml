[package]
name = "xai-crash-handler"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Facade of xai-org/grok-build xai-crash-handler — terminal restore surfaces the pager uses (PR7)"

[dependencies]
