[package]
name = "next-code-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
next-code-message-types = { path = "../next-code-message-types" }
serde = { version = "1", features = ["derive"] }
