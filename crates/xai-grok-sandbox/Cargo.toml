[package]
name = "xai-grok-sandbox"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Facade of xai-org/grok-build xai-grok-sandbox — profile surfaces the pager uses (PR7)"

[dependencies]

[features]
default = []
# Empty: upstream enforces OS sandboxing behind this feature. Stub never
# enforces; feature exists so pager `sandbox-enforce` resolves.
enforce = []
