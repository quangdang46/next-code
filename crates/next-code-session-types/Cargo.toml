[package]
name = "next-code-session-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
next-code-message-types = { path = "../next-code-message-types" }
memchr = "2"
serde = { version = "1", features = ["derive"] }
