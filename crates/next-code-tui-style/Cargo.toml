[package]
name = "next-code-tui-style"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
next-code-core = { path = "../next-code-core" }
ratatui = "0.30"
dark-light = "2"
serde = { version = "1", features = ["derive"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
