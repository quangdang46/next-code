[package]
name = "xai-hooks-plugins-types"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Shared DTO types for hooks/plugins ACP extensions (vendored from xai-org/grok-build for PR7)"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
