[package]
name = "xai-grok-config"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Shim: grok_home → NEXT_CODE_HOME / ~/.next-code for Face (Grok UI PR3)"

[dependencies]
dirs = "6"
dunce = "1"
toml = "0.8"
toml_edit = { version = "0.22", features = ["serde"] }

[dev-dependencies]
tempfile = "3"
