{
  "name": "hello-plugin",
  "version": "0.1.0",
  "description": "A real working example plugin for next-code — exercises nextcode.on, nextcode.registerTool, nextcode.logger, nextcode.kv, nextcode.uuid",
  "nextcode": {
    "name": "Hello Plugin",
    "package_name": "hello-plugin",
    "version": "0.1.0",
    "kind": "server",
    "entry": { "server": "index.ts" },
    "description": "Real working example — see index.ts for what it actually does."
  }
}
