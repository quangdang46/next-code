{
  "name": "hello-plugin",
  "version": "0.1.0",
  "description": "A real working example plugin for next-code — exercises pi.on, pi.registerTool, pi.logger, pi.kv, pi.uuid",
  "jcode": {
    "name": "Hello Plugin",
    "package_name": "hello-plugin",
    "version": "0.1.0",
    "kind": "server",
    "entry": { "server": "index.ts" },
    "description": "Real working example — see index.ts for what it actually does."
  }
}
